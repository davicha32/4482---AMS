@model IEnumerable<AMS.Models.Device>

@{
    ViewData["Title"] = "Archived Devices";
}

<h2>Archived Devices</h2>


<table class="table" id="editItems" data-action="Edit" data-object="/Device">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brand)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr data-id="@item.ID">
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Brand.Name)
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-area="" asp-controller="Device" asp-action="Index">Active Devices</a>

<script defer src="~/js/tableRow.js"></script>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script>
        $(document).ready(function () {
        });
    </script>
}